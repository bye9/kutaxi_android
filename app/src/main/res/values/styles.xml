<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- <item name="colorPrimary">#002196F3</item> -->
        <item name="colorPrimary">#FFFFFF</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorPrimaryDark">#FFFFFF</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name ="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@android:color/black</item>

    </style>

    <style name="Divider">
        <item name="android:layout_width">@dimen/divider_Style_layout_width</item>
        <item name="android:layout_height">@dimen/divider_Style_layout_height</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

</resources>

    <!-- 에니메이션 작업 표시줄 아이콘 구현-->
    <!-- colorprimary - 액션바 색상 -->
    <!-- colorprimarydark - 상태바 색상 -->
    <!-- coloraccent - 강조 색상(위젯에 적용되는 색상) -->

